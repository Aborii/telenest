/**
 * @file src/bots/greeter/greeter.constants.ts
 *
 * PURPOSE
 * -------
 * Constants used by greeter updates, scene handlers, and wizard handlers.
 *
 * USAGE
 * -----
 * import { RANDOM_NUMBER_SCENE_ID } from './greeter.constants';
 */

/** Scene id for the random number scene. */
export const RANDOM_NUMBER_SCENE_ID = 'random-number-scene';

/** Wizard id for the profile collection flow. */
export const PROFILE_WIZARD_ID = 'profile-wizard';

/** Prompt displayed when entering the random-number scene. */
export const RANDOM_SCENE_PROMPT =
  'Type again for another random number or /exit.';
